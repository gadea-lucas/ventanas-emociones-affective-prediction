# Features con solapamiento

Carpeta de salida para las características generadas usando ventanas temporales con solapamiento.

Estos archivos son resultados intermedios y no se versionan en Git. Se generan ejecutando `NB_01_dataset_extractor.ipynb`.
